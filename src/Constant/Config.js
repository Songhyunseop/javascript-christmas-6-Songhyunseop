const ORDER = Object.freeze({
  MENU_NAME: 0,
  COUNT: 1,
});

const DAY = {
  WEEKDAY_END: 5,
};

const REGEX = {
  SPECIAL_SYMBOLS: /[!@#$%^&*(),.?":{}|<>]/,
  NUMBER: /^\d+$/,
};

export { ORDER, DAY, REGEX };
